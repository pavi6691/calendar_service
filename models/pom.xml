<project>
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.acme.calendar</groupId>
        <artifactId>calendar-parent</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>calendar-models</artifactId>

    <name>Calendar models</name>
    <description>Calendar models ...</description>

    <dependencies>

        <!-- Calendar -->
        <dependency>
            <groupId>com.acme.calendar</groupId>
            <artifactId>calendar-core</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

</project>
